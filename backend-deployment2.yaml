---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend2
spec:
  selector:
    matchLabels:
      deployment: hello
      # app: deployment2
  replicas: 5
  template:
    metadata:
      labels:
        deployment: hello
        # app: deployment2
    spec:
      containers:
        - name: hello2
          image: "tutum/hello-world:latest"
          ports:
            - name: http
              containerPort: 80
